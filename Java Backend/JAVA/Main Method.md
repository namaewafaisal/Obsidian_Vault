# Main Method in Java

The main method is the entry point of every Java application.

**Syntax:**
```java
public static void main(String[] args) { }
```
- `public` → accessible to JVM from anywhere
- `static` → can be called without creating an object
- `void` → doesn’t return any value
- `String[] args` → stores command-line arguments

Only one file in a program needs the main method; other classes are linked and used by it.

Related Notes:

- [Java Compilation Process](Java%20Compilation%20Process.md)
-  [Class and Object](OOP/Class%20and%20Object.md)